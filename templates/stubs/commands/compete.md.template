Run a competitive evaluation comparing Dragonfly-assisted vs vanilla Claude Code. Call dragonfly_get_workflow with command="compete" and args="${ARGUMENTS}".
Follow the returned instructions exactly.
